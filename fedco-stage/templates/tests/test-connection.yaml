apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fedco.fullname" . }}-test-connection"
  labels:
    {{- include "fedco.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fedco.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
